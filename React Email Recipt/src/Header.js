import React from 'react';

const Header = (props) => {
    return(
        <div className="Header">
            <div className="wrapper">
                {this.text}
            </div>
        </div>
    );
}

export default Header;